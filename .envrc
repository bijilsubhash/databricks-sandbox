# direnv config -- loads .env into the shell on `cd` into this repo.
# Run `direnv allow` once after copying .env.example -> .env.
# (Optional: `just` also loads .env on its own, so direnv is a convenience for
#  running raw `terraform` / `databricks` / `aws` commands with vars pre-set.)

dotenv_if_exists .env
